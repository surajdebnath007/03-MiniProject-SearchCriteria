server:
  port: 9090
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    password: sa
    username: sa
  jpa:
    properties:
      hibernate:
         show_sql: true
    defer-datasource-initialization: true
